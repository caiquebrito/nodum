{
  "name": "@caiquebrito/nodum-query",
  "version": "2.17.3",
  "description": "Transport-neutral graph-query layer for nodum — shared by the MCP server and future LSP server",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@caiquebrito/nodum-core": "^2.17.3",
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
